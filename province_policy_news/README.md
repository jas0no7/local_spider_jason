# 职教大脑

---

#### 运行

> cmd: scrapy crawl *** 或者 python start.py -n ***    
> docker-compose: sudo docker-compose config --services | grep '^spider_province_policy_news_' | xargs docker-compose up -d
---

#### 使用前请确认206服务器Windows Server正常使用

---